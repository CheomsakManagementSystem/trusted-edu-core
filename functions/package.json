{
  "name": "trusted-edu-core-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js && node --check reportAutoMatcher.js && node --check reportAutoMatchJob.js && node --check stripReportRawText.js",
    "cleanup:report-raw-text": "node stripReportRawText.js",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  }
}
